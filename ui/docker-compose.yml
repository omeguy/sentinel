services:
  sentinel-frontend:
    image: nginx:alpine
    container_name: sentinel-frontend
    ports:
      - "80:80"
    volumes:
      - .:/usr/share/nginx/html:ro
    networks:
      - sentinel-network

networks:
  sentinel-network:
    driver: bridge  # <--- Change 'external: true' to this